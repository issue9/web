# web.yaml 的测试文件，需要与其它内容数值相同

http:
    port: :8082
    readTimeout: 3s
timezone: Africa/Addis_Ababa
mimetypes:
    - type: application/json
      target: json
    - type: text/xml
      target: xml
    - type: multipart/form-data
      target: nil
