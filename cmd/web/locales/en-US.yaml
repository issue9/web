id: und
messages:
    - key: '%s at %s:%d'
      message:
        msg: '%s at %s:%d'
    - key: '%s can not convert to ast.StructType'
      message:
        msg: '%s can not convert to ast.StructType'
    - key: '%s when exec %s, use the preset value %s'
      message:
        msg: '%s when exec %s, use the preset value %s'
    - key: |
        Auxiliary tool for github.com/issue9/web

        commands：
        {{commands}}

        flags：
        {{flags}}

        visit https://github.com/issue9/web for more info.
      message:
        msg: |
            Auxiliary tool for github.com/issue9/web

            commands：
            {{commands}}

            flags：
            {{flags}}

            visit https://github.com/issue9/web for more info.
    - key: build go source
      message:
        msg: build go source
    - key: build usage
      message:
        msg: build usage
    - key: cancelled
      message:
        msg: cancelled
    - key: command %s not found
      message:
        msg: command %s not found
    - key: dest locale files
      message:
        msg: dest locale files
    - key: 'dup # restdoc note'
      message:
        msg: 'dup # restdoc note'
    - key: extract locale
      message:
        msg: extract locale
    - key: |
        extract usage

        flags：
        {{flags}}
      message:
        msg: |
            extract usage

            flags：
            {{flags}}
    - key: file format
      message:
        msg: file format
    - key: filter by tag
      message:
        msg: filter by tag
    - key: gen restdoc
      message:
        msg: gen restdoc
    - key: ignore %s
      message:
        msg: ignore %s
    - key: invalid format
      message:
        msg: invalid format
    - key: invalid http method %s
      message:
        msg: invalid http method %s
    - key: language
      message:
        msg: language
    - key: locale func
      message:
        msg: locale func
    - key: no src dir
      message:
        msg: no src dir
    - key: not found %s
      message:
        msg: not found %s
    - key: only support yaml and json
      message:
        msg: only support yaml and json
    - key: out dir
      message:
        msg: out dir
    - key: package %s with the same name.
      message:
        msg: package %s with the same name.
    - key: parse %s complete
      message:
        msg: parse %s complete
    - key: parse module dependencies
      message:
        msg: parse module dependencies
    - key: recursive dir
      message:
        msg: recursive dir
    - key: restdoc usage
      message:
        msg: restdoc usage
    - key: set output file
      message:
        msg: set output file
    - key: show current help info
      message:
        msg: show current help info
    - key: show help
      message:
        msg: show help
    - key: show version
      message:
        msg: show version
    - key: skip sub module
      message:
        msg: skip sub module
    - key: src locale file
      message:
        msg: src locale file
    - key: start parse %s ...
      message:
        msg: start parse %s ...
    - key: syntax error
      message:
        msg: syntax error
    - key: the dest file %s is dir
      message:
        msg: the dest file %s is dir
    - key: the key %s of %s not found, will be deleted
      message:
        msg: the key %s of %s not found, will be deleted
    - key: unsupported ast expr %+v
      message:
        msg: unsupported ast expr %+v
    - key: unsupported generics type %s
      message:
        msg: unsupported generics type %s
    - key: unsupported marshal for %s
      message:
        msg: unsupported marshal for %s
    - key: unsupported unmarshal for %s
      message:
        msg: unsupported unmarshal for %s
    - key: update locale file
      message:
        msg: update locale file
    - key: |
        update locale file usage

        flags:
        {{flags}}
      message:
        msg: |
            update locale file usage

            flags:
            {{flags}}
    - key: version incompatible
      message:
        msg: version incompatible
