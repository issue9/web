openapi: 3.1.0
info:
  title: test
  version: 1.0.0
components:
  schemas:
    error:
      type: string
paths:
  /pet:
    get:
      responses:
        "200":
          description: 200 response
          content:
            "application/json":
              schema:
                type: string
